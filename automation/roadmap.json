{
  "_comment": [
    "The backlog `lf build` works through. Each item becomes one codegen call,",
    "applied and then gated by the headless tests; a failure is reverted and the",
    "reason lands in `note`.",
    "",
    "Ordered so earlier items don't depend on later ones. `files` narrows the",
    "",
    "Items 9-19 (2026-09-11) expand the game: roster, maps, specials, grabs, air",
    "combat, wider bindings. Ground rules for every one of them:",
    "  * Logic goes in a pure module testable under `node --test`; DOM only renders it.",
    "  * A new module is not finished until main.js (or something it imports) imports",
    "    it \u2014 tools/wiring.test.mjs fails on orphans.",
    "  * tools/module-graph.test.mjs links the whole renderer against the VENDORED",
    "    three.js, so a bad named import fails the gate instead of the app."
  ],
  "items": [
    {
      "id": 1,
      "title": "Add a round timer and best-of-3 rounds",
      "detail": "A 60-second countdown in the HUD. When it hits zero, whoever has more health wins the round. First to two rounds wins the match. Between rounds, reset both fighters to their spawn points and refill health, with a short 'ROUND 2' banner. Show round pips next to each health bar. Add tests for the round-win and timeout-win conditions.",
      "files": [],
      "status": "done",
      "note": "Built by hand (the API path was blocked). 60s clock, best of three, round pips, fighters reset between rounds. 9 tests in tools/rounds.test.mjs."
    },
    {
      "id": 2,
      "title": "Add input buffering and a combo counter",
      "detail": "Buffer an attack press for ~150ms so a button hit slightly before the current move ends still registers, which is what makes chained attacks feel good. Track consecutive hits landed on a fighter within 1.2s of each other as a combo, and show the count in the HUD near that fighter's health bar. Reset on timeout or when the fighter recovers.",
      "files": [
        "little-fighters-js/renderer/src/fighter.js",
        "little-fighters-js/renderer/src/hud.js",
        "little-fighters-js/renderer/src/config.js",
        "little-fighters-js/renderer/src/main.js"
      ],
      "status": "done",
      "note": "Add a configurable 150ms, single-slot buffer for punch/kick presses, consumed as soon as the state machine permits without cancelling moves. Track unblocked, damaging hits on each defender, clearing t"
    },
    {
      "id": 3,
      "title": "Make the camera track both fighters",
      "detail": "Instead of a fixed camera, keep both fighters framed: follow the midpoint on X/Z and pull back as they separate, clamped so it never leaves the arena or gets closer than the current framing. Smooth it with damping so it never snaps. Keep the same pitch and field of view.",
      "files": [
        "little-fighters-js/renderer/src/arena.js",
        "little-fighters-js/renderer/src/main.js",
        "little-fighters-js/renderer/src/config.js"
      ],
      "status": "done",
      "note": "Extend the existing camera update to follow the fighters\u2019 midpoint on both X and Z, with arena-clamped tracking offsets and bounded pull-back along the camera\u2019s viewing axis. Damp tracking and pull-ba"
    },
    {
      "id": 4,
      "title": "Add a main menu and pause screen",
      "detail": "A title screen before the fight with 'Fight the CPU', 'Two players' and a difficulty selector (which sets the AI's base aggression and think interval). Escape pauses mid-fight with resume and quit-to-menu. The game loop should stop advancing while paused. Keep it all in the existing DOM overlay style rather than rendering menus in three.js.",
      "files": [],
      "status": "done",
      "note": "Add a DOM-based title/pause menu and a simulation gate that freezes every gameplay update while paused, without stopping rendering. Add configurable difficulty presets and per-controller AI settings;  [correction] menu.js was written but never imported by main.js, so the feature was not in the game. Wired in by hand on 2026-09-11, along with input.clearInput() and a match lifecycle. tools/wiring.test.mjs now fails on any orphaned module."
    },
    {
      "id": 5,
      "title": "Add a second local player option",
      "detail": "Let the second fighter be driven by keyboardController('p2') instead of the AI, chosen from the menu. The p2 bindings already exist in input.js. Make sure the AI is not constructed at all in two-player mode so no API calls are made.",
      "files": [
        "little-fighters-js/renderer/src/main.js",
        "little-fighters-js/renderer/src/input.js",
        "little-fighters-js/renderer/src/ai.js"
      ],
      "status": "done",
      "note": "Add a startup menu for CPU or local two-player matches. Local mode uses `keyboardController('p2')` and never constructs the AI or calls the model bridge, including status and usage polling. CPU behavi"
    },
    {
      "id": 6,
      "title": "Add light and heavy attack variants",
      "detail": "Split the punch into a light jab (faster, less damage, less recovery) and a heavy punch (slower, more damage, more knockback) on separate buttons. Reuse the existing punch clip for both with different playback speeds until dedicated clips exist. Extend the AI's attack vocabulary to include them.",
      "files": [
        "little-fighters-js/renderer/src/fighter.js",
        "little-fighters-js/renderer/src/config.js",
        "little-fighters-js/renderer/src/input.js",
        "little-fighters-js/renderer/src/ai.js",
        "little-fighters-js/renderer/src/main.js",
        "little-fighters-js/tools/fighter.test.mjs"
      ],
      "status": "done",
      "note": "Add light and heavy punches on separate buttons, with configurable damage, knockback, and playback speeds that also scale startup and recovery. Both reuse `punch`; buffering, airborne punches, and per"
    },
    {
      "id": 7,
      "title": "Add hit-stop and a KO slow-motion finish",
      "detail": "Freeze both fighters for ~60ms when a heavy hit lands, which is what gives impacts weight. On the killing blow, drop the global time scale to about 0.35 for a second and push the camera slightly closer before the KO banner. Implement as a time-scale multiplier applied in the game loop so nothing else needs to know about it.",
      "files": [
        "little-fighters-js/renderer/src/main.js",
        "little-fighters-js/renderer/src/fighter.js",
        "little-fighters-js/renderer/src/arena.js",
        "little-fighters-js/renderer/src/hud.js",
        "little-fighters-js/renderer/src/config.js"
      ],
      "status": "done",
      "note": "Add a loop-owned time multiplier: confirmed, unblocked heavy punches freeze simulation for 60 ms; lethal hits then play one second at 0.35\u00d7 speed before round resolution and the KO banner. Add a small"
    },
    {
      "id": 8,
      "title": "Add procedural sound effects",
      "detail": "Use the Web Audio API to synthesise the sounds rather than shipping audio files: a short noise burst with a low thump for a landed hit, a duller filtered version for a blocked one, a whoosh for a whiffed swing, a thud on landing from a jump. Keep it in a new renderer/src/audio.js with a single play(name) entry point, and a master mute toggle on M.",
      "files": [
        "little-fighters-js/renderer/src/main.js",
        "little-fighters-js/renderer/src/fighter.js",
        "little-fighters-js/renderer/src/config.js"
      ],
      "status": "done",
      "note": "Fix audio unlocking by removing the promise-based resume latch: it can remain set after the context is running and incorrectly prevent a later gesture from resuming suspended audio. Each gesture now r"
    },
    {
      "id": 9,
      "title": "Give each fighter its own stats",
      "detail": "Fighter reads speed, health and damage from module constants in config.js, so every fighter is identical and no roster is possible. Add an optional `stats` option to the Fighter constructor \u2014 maxHealth, walkSpeed, runSpeed, jumpSpeed, damageScale, defenceScale \u2014 stored on the instance and read everywhere the constants are read now. Omitting it must reproduce today's numbers exactly, so existing tests keep passing unchanged. damageScale multiplies damage this fighter deals; defenceScale divides damage it receives. Add tests proving: a fighter with damageScale 2 deals double, one with defenceScale 2 takes half, a faster walkSpeed covers more ground in the same dt, and a fighter built with no stats matches the config constants field for field.",
      "files": [
        "little-fighters-js/renderer/src/config.js",
        "little-fighters-js/renderer/src/fighter.js",
        "little-fighters-js/renderer/src/main.js"
      ],
      "status": "done",
      "note": "The stats implementation and its tests are already present. The reported failures come from macOS AppleDouble metadata files (`._*.js`) being treated as JavaScript modules. Add a pretest cleanup that "
    },
    {
      "id": 10,
      "title": "Add a character roster",
      "detail": "New module renderer/src/characters.js. Export CHARACTERS: at least six fighters, each { id, name, tagline, tint, scale, stats, special }, where stats matches the shape item 9 added and special is a string id for now. Make the archetypes genuinely different \u2014 balanced, heavy and slow with high damage, fast and fragile, long-reach, defensive wall, glass cannon \u2014 by varying the stats, not just the names. Export characterById(id) falling back to the first character, and applyCharacter(model, character) which clones every material it touches before tinting, so two fighters never share a material instance, and applies scale. Pure data plus small functions: no DOM, and no scene access beyond the model passed in. Import it from main.js and use it when building fighters. Tests: ids unique, six or more, stats inside sane bounds, applyCharacter leaves the source material untouched, and two different characters cloned from one model end up with different material colours.",
      "files": [
        "little-fighters-js/renderer/src/config.js",
        "little-fighters-js/renderer/src/fighter.js",
        "little-fighters-js/renderer/src/assets.js",
        "little-fighters-js/renderer/src/main.js"
      ],
      "status": "done",
      "note": "Add six stat-distinct fighters, with roster tuning in config.js and material-safe appearance helpers in characters.js. Match creation will apply roster names, stats, tint, and scale; defaults are Axel"
    },
    {
      "id": 11,
      "title": "Add four playable arenas",
      "detail": "New module renderer/src/arenas.js. Four maps built procedurally in three.js with no new art files: a dojo (warm wood floor, paper screens, hanging lanterns), a neon street at night (wet asphalt, coloured signage glow, rain-slick reflections), a temple courtyard (stone flags, pillars, dusk sky), and a rooftop (concrete, distant city lights, high fog). Export MAPS as { id, name, floorColor, skyColor, fog, lights, build(scene) } and buildMap(scene, mapId) returning a dispose() that removes every object and light it added and frees their geometries and materials. createArena(canvas, mapId) selects one. Switching map between matches must leave the scene's child count back where it started. Tests: ids unique, unknown id falls back to the first map, and build-then-dispose restores the exact scene child count and disposes what it created.",
      "files": [
        "little-fighters-js/renderer/src/arena.js",
        "little-fighters-js/renderer/src/config.js",
        "little-fighters-js/renderer/src/main.js"
      ],
      "status": "done",
      "note": "Add four procedural arenas with map-owned lighting, shared geometry, and idempotent cleanup. Keep combat bounds, fighters, and camera behavior unchanged. Add an arena selector between matches, safe ma"
    },
    {
      "id": 12,
      "title": "Add a pre-fight selection state machine",
      "detail": "New module renderer/src/select-state.js, pure and DOM-free so it can be tested in node. Export createSelectState({ characters, maps, twoPlayer }) modelling the flow mode -> P1 character -> P2 character -> map -> ready. In one-player mode the CPU picks its own character, so that step is skipped and filled in automatically. Methods: move(delta) to move the cursor, choose() to confirm the highlighted entry, back() to return to the previous step (and from the first step, to report that the caller should leave the flow), plus getters step, cursor, options, selection and canStart. back() must clear the choice it is undoing. Test it exhaustively: every path through the flow, back() from each step, cursor wrapping at both ends, and that selection is only complete when canStart is true.",
      "files": [
        "little-fighters-js/renderer/src/characters.js",
        "little-fighters-js/renderer/src/arena.js",
        "little-fighters-js/renderer/src/config.js"
      ],
      "status": "blocked",
      "note": "tests failed: # pass 119 # fail 1",
      "blocked_by": "tests"
    },
    {
      "id": 13,
      "title": "Show character and map select in the menu",
      "detail": "Render the item-12 state machine in menu.js, between choosing a mode and starting the match: a grid of character cards showing name, tagline and a tint swatch, then a map grid, with the highlighted entry visibly selected. Drive it with arrow keys and Enter as well as the mouse, keep the existing focus trap and Escape behaviour, and add a Back control. onStart must now receive { mode, difficulty, ai, p1Character, p2Character, map } and main.js must use all of them \u2014 build each fighter through applyCharacter with its own stats, and create the arena with the chosen map. Do not put game logic in menu.js; it renders the state machine and nothing more. Every new module must be imported from main.js or something main.js imports, or tools/wiring.test.mjs will fail.",
      "files": [
        "little-fighters-js/renderer/src/menu.js",
        "little-fighters-js/renderer/src/main.js",
        "little-fighters-js/renderer/src/characters.js",
        "little-fighters-js/renderer/src/arena.js",
        "little-fighters-js/renderer/src/config.js"
      ],
      "status": "done",
      "note": "Add a small, headless selection state machine for mode \u2192 P1 character \u2192 P2/CPU character \u2192 map. The menu will render its cards, selection highlight, keyboard navigation and Back control while preservi"
    },
    {
      "id": 14,
      "title": "Add a super meter and per-character specials",
      "detail": "A meter per fighter that fills as it deals and takes damage, capped, and spends in full when a special is thrown. Put the meter maths in its own pure module so it is testable. Give each roster character a distinct special keyed off character.special: a forward projectile, a rising uppercut, a spinning multi-hit kick, a ground pound with a shockwave, a dashing shoulder charge, and a short counter stance that punishes the next hit. Reuse the existing animation clips rather than inventing new ones \u2014 a special is a state with its own timing, damage, reach and cancel rules. Show the meter in the HUD under each health bar. Bind it to a new key per player. Tests: the meter fills and clamps, a special cannot fire below full, firing empties it, and each special's damage and reach differ.",
      "files": [
        "little-fighters-js/renderer/src/fighter.js",
        "little-fighters-js/renderer/src/characters.js",
        "little-fighters-js/renderer/src/config.js",
        "little-fighters-js/renderer/src/hud.js",
        "little-fighters-js/renderer/src/input.js",
        "little-fighters-js/renderer/src/main.js"
      ],
      "status": "blocked",
      "note": "API call failed: stream error: {\"type\": \"error\", \"error\": {\"type\": \"server_error\", \"code\": \"server_error\", \"headers\": {\"skip-error-remapping\": \"true\"}, \"message\": \"The server had an error processing your request. Sorry about that! You can retry your request, or contact us through an Azure support request at: https://go.microsoft.",
      "blocked_by": "api"
    },
    {
      "id": 15,
      "title": "Add grabs and throws",
      "detail": "A grab that only connects at close range against a fighter who is not already being thrown, is not invulnerable, and is not mid-special. On connection the victim enters a held state for a short window during which a direction plus attack throws them forward or backward, and no input escapes it except a mash that breaks out if pressed fast enough. A throw deals damage, sets knockdown, and cannot be comboed into another grab immediately. Blocking does not stop a grab \u2014 that is the point of grabs \u2014 but a grab is slower to start than a jab. Bind it to a new key per player. Tests: range gating, break-out on a fast mash, no break-out on a slow one, throw damage and direction, and that a grab cannot chain into a second grab within the cooldown.",
      "files": [
        "little-fighters-js/renderer/src/fighter.js",
        "little-fighters-js/renderer/src/config.js",
        "little-fighters-js/renderer/src/input.js",
        "little-fighters-js/renderer/src/main.js"
      ],
      "status": "pending",
      "note": ""
    },
    {
      "id": 16,
      "title": "Add air attacks and juggles",
      "detail": "Attacks while airborne: a jumping light, a jumping heavy and a diving kick that commits the fighter downward. A fighter hit while airborne enters a juggle state, staying in hitstun and floating briefly so a follow-up can connect, with a per-combo juggle limit so it cannot loop forever and diminishing damage as the juggle count rises. Landing cancels the air state. Tests: an air attack only starts off the floor, juggle count increments per airborne hit, damage scales down as it rises, the limit ends the juggle, and landing resets it.",
      "files": [
        "little-fighters-js/renderer/src/fighter.js",
        "little-fighters-js/renderer/src/config.js",
        "little-fighters-js/renderer/src/main.js"
      ],
      "status": "pending",
      "note": ""
    },
    {
      "id": 17,
      "title": "Widen the key bindings and generate the on-screen controls",
      "detail": "INPUT_MAPS needs entries for the new actions \u2014 special, grab, taunt \u2014 for both players, chosen so they are comfortable on one keyboard and never collide with each other or with the mute key. Then stop hand-writing the control hints: the text in index.html is already stale, still promising 'J punch' when J is a light jab and I is a heavy. Generate the on-screen hints from INPUT_MAPS at runtime, with a readable label per physical key code, so the two can never drift apart again. Add a test asserting no key code is bound twice within a player's map, that the two players' maps do not overlap, that neither claims the mute key, and that every action in the map has a label.",
      "files": [
        "little-fighters-js/renderer/src/config.js",
        "little-fighters-js/renderer/src/input.js",
        "little-fighters-js/renderer/src/hud.js",
        "little-fighters-js/renderer/src/main.js",
        "little-fighters-js/renderer/index.html"
      ],
      "status": "pending",
      "note": ""
    },
    {
      "id": 18,
      "title": "Bring the camera down to the fight",
      "detail": "The camera sits at y 10, z 9.5, pitched -38 degrees with a 40 degree field of view, which leaves the fighters small in the middle of a mostly empty frame and the top third of the screen showing nothing. Lower it and bring it in so the fighters fill the frame the way they do in a beat-'em-up, keeping the existing follow, separation pull-back and KO push-in behaviour intact. Let each map override the camera so the rooftop can sit lower than the temple. Add a test that the resolved camera for every map keeps both fighters inside the view frustum at maximum separation and at the arena's corners.",
      "files": [
        "little-fighters-js/renderer/src/arena.js",
        "little-fighters-js/renderer/src/config.js"
      ],
      "status": "pending",
      "note": ""
    },
    {
      "id": 19,
      "title": "Load a separate model per character, falling back to the knight",
      "detail": "Right now every character is the same knight with a different tint. Let a character optionally name its own .glb under renderer/assets/characters/, load it once and cache it, and clone per fighter exactly as the knight is cloned. A character with no model, or whose model fails to load, must silently fall back to the tinted knight \u2014 a missing file is never fatal, the same rule the clip loader already follows. Animation clips stay shared, so a model whose skeleton does not match the mixamorig bone names should be rejected at load time and fall back rather than animate wrongly. Tests: fallback on a missing model, fallback on a skeleton mismatch, and that the cache loads a shared model only once.",
      "files": [
        "little-fighters-js/renderer/src/assets.js",
        "little-fighters-js/renderer/src/characters.js",
        "little-fighters-js/renderer/src/config.js",
        "little-fighters-js/renderer/src/main.js"
      ],
      "status": "pending",
      "note": ""
    }
  ]
}
